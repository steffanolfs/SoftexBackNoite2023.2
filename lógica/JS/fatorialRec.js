function fatorialRec(num) {
    if (n === 1) {

    } else {
        return fatorialRec(n - 1) * n;
    }

    var num = parseInt(prompt("Informe um número inteiro positivo: "));



}

console.log(`${num}² = ${fatorialRec}`);